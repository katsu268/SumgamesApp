import { Tile, Text, Button, ThemeProvider, Input, Icon } from 'react-native-elements';
import React ,{ Component }from 'react';
import { View, StyleSheet, ScrollView } from 'react-native';
import { SafeAreaProvider } from 'react-native-safe-area-context';


const Hostmatching = () => {
    return (
      <SafeAreaProvider>
        <ScrollView>
        <Text
            h1
            h1Style={{}}
            h2Style={{}}
            h3Style={{}}
            h4Style={{}}
            style={{}}
        >
      Game Title
    </Text>
        </ScrollView>
      </SafeAreaProvider>
    )
}

export default Hostmatching;